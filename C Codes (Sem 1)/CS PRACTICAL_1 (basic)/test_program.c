//Test Program

# include <stdio.h>
# include <math.h>

void main()
    {
        printf("\nsize of short in VSCODE       = %d", sizeof(short));
        printf("\nsize of int in VSCODE         = %d", sizeof(int));
        printf("\nsize of long in VSCODE        = %d", sizeof(long));

        printf("\nsize of float in VSCODE       = %d", sizeof(float));
        printf("\nsize of double in VSCODE      = %d", sizeof(double));
        printf("\nsize of long double in VSCODE = %d", sizeof(long double));
    }